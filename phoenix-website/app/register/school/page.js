import RegistrationForm from "@/components/RegistrationForm";

export const metadata = { title: "School Registration — Phoenix" };

export default function SchoolRegisterPage() {
  return <RegistrationForm type="school" />;
}
